server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  # ── JSON logger ─────────────────────────────────────────────────────────────
  - job_name: json-app
    static_configs:
      - targets:
          - localhost
        labels:
          job: logstream
          app: json-app
          env: development
          __path__: /logs/json-app.log

    pipeline_stages:
      # Parse the JSON fields so they become queryable labels
      - json:
          expressions:
            level: level
            message: message
      - labels:
          level:

  # ── Syslog logger ────────────────────────────────────────────────────────────
  - job_name: syslog-app
    static_configs:
      - targets:
          - localhost
        labels:
          job: logstream
          app: syslog-app
          env: development
          __path__: /logs/syslog-app.log

    pipeline_stages:
      # Extract level from syslog priority keyword
      - regex:
          expression: '(?P<level>DEBUG|INFO|WARN|ERROR|NOTICE|CRIT)'
      - labels:
          level:

  # ── Plain-text logger ────────────────────────────────────────────────────────
  - job_name: text-app
    static_configs:
      - targets:
          - localhost
        labels:
          job: logstream
          app: text-app
          env: development
          __path__: /logs/text-app.log

    pipeline_stages:
      - regex:
          expression: '\[(?P<level>DEBUG|INFO|WARN|ERROR)\]'
      - labels:
          level:

  # ── Auth Service ──────────────────────────────────────────────────────────
  - job_name: auth-service
    static_configs:
      - targets:
          - localhost
        labels:
          job: logstream
          app: auth-service
          env: development
          __path__: /logs/auth-service.log

    pipeline_stages:
      - json:
          expressions:
            level: level
            message: message
      - labels:
          level:

  # ── Payment Service ───────────────────────────────────────────────────────
  - job_name: payment-service
    static_configs:
      - targets:
          - localhost
        labels:
          job: logstream
          app: payment-service
          env: development
          __path__: /logs/payment-service.log

    pipeline_stages:
      - json:
          expressions:
            level: level
            message: message
      - labels:
          level:

  # ── Order Service ─────────────────────────────────────────────────────────
  - job_name: order-service
    static_configs:
      - targets:
          - localhost
        labels:
          job: logstream
          app: order-service
          env: development
          __path__: /logs/order-service.log

    pipeline_stages:
      - regex:
          expression: '\[(?P<level>DEBUG|INFO|WARN|ERROR)\]'
      - labels:
          level:

  # ── Inventory Service ─────────────────────────────────────────────────────
  - job_name: inventory-service
    static_configs:
      - targets:
          - localhost
        labels:
          job: logstream
          app: inventory-service
          env: development
          __path__: /logs/inventory-service.log

    pipeline_stages:
      - regex:
          expression: '(?P<level>DEBUG|INFO|WARN|ERROR)'
      - labels:
          level:

  # ── API Gateway ───────────────────────────────────────────────────────────
  - job_name: api-gateway
    static_configs:
      - targets:
          - localhost
        labels:
          job: logstream
          app: api-gateway
          env: development
          __path__: /logs/api-gateway.log

    pipeline_stages:
      - json:
          expressions:
            level: level
            message: message
      - labels:
          level:

  # ── Notification Service ──────────────────────────────────────────────────
  - job_name: notification-service
    static_configs:
      - targets:
          - localhost
        labels:
          job: logstream
          app: notification-service
          env: development
          __path__: /logs/notification-service.log

    pipeline_stages:
      - json:
          expressions:
            level: level
            message: message
      - labels:
          level:

  # ── Analytics Service ─────────────────────────────────────────────────────
  - job_name: analytics-service
    static_configs:
      - targets:
          - localhost
        labels:
          job: logstream
          app: analytics-service
          env: development
          __path__: /logs/analytics-service.log

    pipeline_stages:
      - json:
          expressions:
            level: level
            message: message
      - labels:
          level:
